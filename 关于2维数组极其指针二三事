int ** 二级指针单元中存放一级指针的地址 指向一级指针int *
int *[] 指针数组 它首先是一个数组，数组中每个单元存放一个指针
int (*)[] 数组指针 它首先是一个指针，一个指针，一个指针，重要的事情说三遍，作为一个指针它存放的是地址，地址指向的是三个int类型的元素一起构成的一维数组
int (*)[][] 数组指针 ，同上 只不过，它存放地址指向的是二维数组。

int [][] 往函数里传二维数组的时候就是上面的数组指针类型类型
